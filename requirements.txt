# =============================================================================
# OceanWatch AI — Python Requirements
# =============================================================================
# Install: pip install -r requirements.txt
# Python 3.10+ recommended
# =============================================================================

# -----------------------------------------------------------------------------
# Core data science
# -----------------------------------------------------------------------------
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0

# -----------------------------------------------------------------------------
# Machine learning
# -----------------------------------------------------------------------------
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.1.0          # optional alternative booster

# -----------------------------------------------------------------------------
# Deep learning (choose one framework; both listed for flexibility)
# -----------------------------------------------------------------------------
torch>=2.2.0              # PyTorch — primary DL framework
torchvision>=0.17.0       # image transforms / pretrained CNNs
# tensorflow>=2.15.0      # TensorFlow — uncomment if preferred

# -----------------------------------------------------------------------------
# Computer vision / image processing
# -----------------------------------------------------------------------------
opencv-python>=4.9.0.80
Pillow>=10.2.0
rasterio>=1.3.9           # GeoTIFF / satellite image I/O
earthpy>=0.9.4            # Earth data helpers

# -----------------------------------------------------------------------------
# Geospatial
# -----------------------------------------------------------------------------
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
fiona>=1.9.0
netCDF4>=1.6.5            # NetCDF satellite data format
xarray>=2024.1.0          # labelled multi-dim arrays (NetCDF, GRIB)
cartopy>=0.22.0           # map projections (optional, heavy)

# -----------------------------------------------------------------------------
# Remote sensing / data access
# -----------------------------------------------------------------------------
sentinelsat>=1.2.1        # Copernicus Sentinel API
erddapy>=2.2.0            # NOAA ERDDAP data access
cdsapi>=0.6.1             # Copernicus Climate Data Store (ERA5)
pyhdf>=0.11.0             # HDF4 for NASA MODIS data

# -----------------------------------------------------------------------------
# Backend — FastAPI
# -----------------------------------------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9   # file uploads
httpx>=0.27.0             # async HTTP client

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
sqlalchemy>=2.0.28
asyncpg>=0.29.0           # async PostgreSQL driver
alembic>=1.13.1           # DB migrations
psycopg2-binary>=2.9.9    # sync PostgreSQL driver (fallback)
geoalchemy2>=0.14.6       # PostGIS spatial types for SQLAlchemy
# pymongo>=4.6.0          # MongoDB driver — uncomment if using Mongo

# -----------------------------------------------------------------------------
# Visualisation
# -----------------------------------------------------------------------------
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.20.0
folium>=0.16.0            # Python-side Leaflet maps

# -----------------------------------------------------------------------------
# Alerting / utilities
# -----------------------------------------------------------------------------
celery>=5.3.6             # background task queue (optional)
redis>=5.0.3              # Celery broker (optional)
python-dotenv>=1.0.1      # .env file loading
requests>=2.31.0
tqdm>=4.66.2              # progress bars
loguru>=0.7.2             # structured logging
pyyaml>=6.0.1

# -----------------------------------------------------------------------------
# Testing
# -----------------------------------------------------------------------------
pytest>=8.1.0
pytest-asyncio>=0.23.5
httpx>=0.27.0             # async test client for FastAPI

# -----------------------------------------------------------------------------
# Notebooks
# -----------------------------------------------------------------------------
jupyter>=1.0.0
ipykernel>=6.29.0
ipywidgets>=8.1.0
